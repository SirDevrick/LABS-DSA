#include <stdio.h>
#include <stdlib.h>

#define RED 0
#define BLACK 1

struct node {
  int data;
  int color;
  struct node *parent;
  struct node *left;
  struct node *right;
};

int main() { return 0; }
